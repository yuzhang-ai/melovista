# 四景 · 六音区沉浸式键盘钢琴

使用电脑键盘演奏覆盖 C1–B6 的浏览器乐器。页面同时显示四组缩短后的真实钢琴键盘：C4、C5 固定，低音组在 C2/C3 间切换，扩展组在 C1/C6 间切换。可在海岸午后、林间晴窗、雨夜公寓与星空露台四个原创场景间切换。

声音使用 Web Audio API 直接调度，音频先启动、视觉粒子后创建。采样加载并解码到内存后，演奏热路径不再请求网络。场景背景使用压缩后的本地 WebP 资源，不参与音频调度。

所有琴键产生的光尘都会抵达相同最高处，低音与高音只改变声音，不再改变粒子上升终点。场景与音色均使用自定义玻璃弹层，避免浏览器原生下拉框破坏沉浸感。

## 启动

```powershell
npm.cmd install
npm.cmd run dev
```

打开终端显示的本地地址，选择音色，点击一次右上角的“加载并启动”，再使用键盘弹奏。

## 沉浸模式

点击顶部的“沉浸演奏”，页面会屏蔽无关按键，仅保留 48 个琴键、`Space` 低音区切换、小键盘 `+` 扩展音区切换和左 `Alt` 长短音切换。退出沉浸模式使用页面按钮。支持 Keyboard Lock 的浏览器会同时尝试全屏键盘锁定；系统保留快捷键仍可能由操作系统接管。

## 音色

- 原声：Salamander Grand Piano 原声采样
- 明亮：同一钢琴采样叠加高频搁架滤波
- 小提琴：真实弓弦采样
- 钢丝弦吉他：真实拨弦采样
- 萨克斯：真实管乐采样

原声与明亮钢琴共用一套约 1.2MB 的采样；其余音色仅在第一次选择时按需加载，避免拖慢首次打开。

## 键位

- C4–B4 全音：`· 1 2 3 4 5 6`
- C4–B4 半音：`Tab Q E R T`
- C5–B5 全音：`7 8 9 0 - = Backspace`
- C5–B5 半音：`U I P [ ]`
- C3–B3 全音：`A S D F G H J`
- C3–B3 半音：`Z X V B N`
- C1–B1 全音：`Insert Home PageUp NumLock Numpad / * -`
- C1–B1 半音：`Delete End Numpad 7 8 9`
- `Space`：将最低一组在 C3–B3 与 C2–B2 间切换
- 小键盘 `+`：将扩展组在 C1–B1 与 C6–B6 间切换；切换到 C6 时该组移动到键盘最右侧
- 左侧 `Alt`：在短音与长音之间切换；短音尾音约 2–3 秒，长音尾音最长约 9 秒；右侧 Alt 不响应

页面会显示最近一次键盘事件到音频调度的 JS 耗时、最近 120 次 P95，以及浏览器报告的基础音频延迟。JS 调度耗时不等于扬声器最终出声的端到端延迟，最终听感需要使用内置扬声器或有线耳机人工确认。

## 采样授权

音源来自 Alexander Holm 的 Salamander Grand Piano V3，采用 CC BY 3.0 许可。本项目使用覆盖 C1–B6 的压缩采样子集，并在页面中保留署名。

小提琴、钢丝弦吉他和萨克斯采样来自 tonejs-instruments，采用 CC BY 3.0 许可；原始采样来源分别为 VSO2、University of Iowa Electronic Music Studios 与 Karoryfer。详见 `public/audio/instruments/LICENSE.txt`。

## 验证

```powershell
npm.cmd test
```

发布前还需使用内置扬声器或有线耳机人工弹奏，确认真实端到端延迟、和弦并发与长短音尾音符合听感预期。

后续“自动听曲并可同时跟弹”的产品与技术设计见 `docs/APPRECIATION_MODE.md`。
