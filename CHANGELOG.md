# 更新日志

本项目从 `v0.2.0` 起遵循语义化版本，并为每次公开版本保留 Git Tag。

## [0.16.1] - 2026-07-21

### 《花海》线上资源刷新

- 为精修后的《花海》MIDI 增加内容哈希版本标识，避免浏览器继续复用精修前的同名缓存文件。
- 线上验收增加《花海》曲库入口与 MIDI 文件哈希比对，确保主站、备用站和本地版本完全一致。

## [0.16.0] - 2026-07-21

### 节拍器

- 新增独立练习面板，提供 `40–208 BPM` 连续调节、五档常用速度预设和 `2/4`、`3/4`、`4/4`、`6/4` 拍号。
- 每小节第一拍使用更高音高和更强包络提示重拍，当前拍通过同步视觉圆点显示。
- 节拍声复用 Web Audio 音频时钟，采用 25ms lookahead 与 100ms 提前排程，避免普通 JavaScript 定时器直接出声造成的节拍漂移。
- 支持读取当前 MIDI 的首个 BPM，并按时间轴覆盖长度选择主拍号；《花海》的两拍弱起不会让全曲误同步为 2/4。页面隐藏或组件卸载时自动停止并清理节拍器。

### A–B 循环练习

- 播放过程中可用当前进度设置 A 点和 B 点，并随时启用、停用或清除片段循环。
- 调度窗口在 B 点截断，抵达 B 点后释放已排程声部并从 A 点重新进入现有 AudioContext 时间轴。
- 切歌、自动连播和导入新本地曲目时清除旧循环，避免将上一首的时间点错误套用到下一首。

### 验证

- 自动化检查覆盖节拍提前排程、重拍包络、BPM/拍号控制、曲目同步、A–B 截断与回跳、后台清理。
- 真实节拍稳定性、扬声器音量和手机端操作手感保留人工试听确认闭环。

## [0.15.0] - 2026-07-20

### 本地 MIDI 音乐库

- 单文件临时槽升级为会话级本地曲库，支持多选文件、选择整个文件夹和拖拽导入。
- 使用文件夹相对路径区分同名曲目，批量提取曲名、时长、BPM 和音符数量，并提供曲名/路径搜索与一键清空。
- 导入采用逐文件容错：损坏或空 MIDI 会被记录并跳过，不阻断同批其他曲目；文件全程只在浏览器读取，不上传服务器。
- 本地曲目加入统一播放队列，上一首、下一首、顺序播放与随机播放均可在内置曲目和本地曲库之间连续工作。

### 鼠标与触摸演奏

- 六个可视音区全部支持鼠标点击、触摸、多点和滑奏，首次触摸可直接唤起音频引擎。
- 使用 Pointer Events、指针捕获和坐标命中检测统一桌面与移动端交互；每个触点维护独立声部，抬起、取消、失焦时均安全释放。
- 电脑键盘映射规则保持不变，触控与电脑键盘、自动演奏可以同时发声并分别显示琴键状态。

### 验证

- 自动化检查覆盖文件夹输入、本地零上传、坏文件容错、动态队列、六音区 MIDI 标记和多点滑奏事件。
- 通过 ESLint、Vinext 生产构建与服务端渲染测试；真实手机多点手感保留人工体验确认闭环。

## [0.14.1] - 2026-07-20

### 《花海》MIDI 精修

- 将原先按整页秒数缩放的生成方式改为逐小节 tick 级重建：开头保留两拍弱起，之后第 1–49 小节全部回到稳定 4/4 时间轴。
- 修复 OMR 在第 10、16、26、33、34、38、41、48 小节产生的声部游标错位、尾奏节奏拉长与跨页累计漂移。
- 恢复谱面第 41–48 小节的 `8va`，让右手旋律按原谱升高八度；同时清理零时长和同拍同音重复事件。
- 新版 MIDI 保持双手两轨和 `♩=75`，包含 769 个有效音符，总时长 `2:38.4`，音域为 C♯2–A6。

### 验证

- 通过 MIDI 结构检查确认两拍弱起、4/4 正拍、49 个完整小节、双轨、无空小节、无同轨同拍同音重复事件。
- 使用 MuseScore 将修复后的 MIDI 反向排谱，人工核对三页结构、分页连接、尾奏延音与高八度段落。

## [0.14.0] - 2026-07-20

### 播放模式

- 将原单曲循环按钮升级为顺序播放、单曲循环、随机播放三种模式，按钮直接显示当前模式并支持中英文界面与无障碍名称。
- 顺序播放在当前曲目结束后自动进入下一首，并在曲库末尾回到第一首。
- 随机播放从其他内置曲目中选择下一首，避免连续抽到当前曲目；本地导入 MIDI 播放完成后也可继续进入内置曲库。

### 工程

- 自动连播复用现有 MIDI 解析缓存、音频时钟调度和琴键光尘，不改变自由演奏低延迟热路径。
- 自动化检查覆盖三种模式文案、模式切换、顺序选曲、随机排除当前曲目和自动启动下一首。

## [0.13.0] - 2026-07-20

### 《花海》曲库

- 根据项目所有者提供的三页完整钢琴谱图片，在本地使用 OMR 识别并生成《花海》双手钢琴 MIDI，不向第三方识谱网站上传谱面。
- 识别范围覆盖引子与第 1–50 小节，补正 A 大调、4/4 拍和 `♩=75`，将三页统一为连续时间轴。
- 生成文件包含左右手两条钢琴轨、787 个音符，总时长约 `2:41.6`，音域为 C♯2–E6，可由现有六音区采样完整播放。
- 曲库新增中英文曲名与作者信息，支持自动演奏、变速、循环、琴键同步和用户同时跟弹。

### 验证

- 自动化检查增加《花海》文件解析、双轨结构、音符数、速度、总时长、音域和曲库入口验证。
- Lint 明确排除本地 `work/` 临时工具目录，避免 Audiveris、MuseScore 等第三方脚本进入项目源码检查范围。
- 保留人工试听闭环；若发现局部识谱偏差，可按具体小节继续精修 MIDI。

## [0.12.0] - 2026-07-19

### Vercel 主站

- 增加标准 Next.js 的 Vercel 构建通道，保留现有 Sites/Vinext 构建，项目可同时发布到两个平台。
- Open Graph 等绝对资源地址优先使用 Vercel 生产域名，并为首次部署提供 `melovista.vercel.app` 默认地址。
- 新增 `vercel.json`，固定 Next.js 框架识别和生产构建命令。

### 开源署名

- 页面底部加入轻量 GitHub 署名入口，跳转到 `yuzhang-ai/melovista`；中文、英文界面均提供对应无障碍说明。
- 窄屏仅保留紧凑的 `GitHub ↗` 标记，避免遮挡琴键提示与沉浸画面。

### 工程

- 增加 Vercel 配置、双构建通道、生产域名和 GitHub 跳转的自动化验证。
- 为 Next.js 构建排除仅由 Cloudflare Workers 使用的入口，避免平台专属类型影响 Vercel 发布。

## [0.11.3] - 2026-07-19

### 雨夜响度校准

- 使用 EBU R128 实测四条环境音：雨夜约为 `-27.5 LUFS`，比海景与城市场景高约 23–25dB，确认上一版仅使用运行时 `0.42` 系数仍然过响。
- 在素材层将雨夜环境音降低 `27.5dB`，重制后约为 `-55.0 LUFS`，让持续雨声回到钢琴背后的白噪音位置。
- 雨夜环境音更换为 `rain-night-v2.wav`，强制浏览器和 CDN 请求新资源，避免继续复用旧缓存。

### 工程

- 自动化验证增加雨夜版本化资源与 PCM 有效响度比较，防止后续回归为高响度素材。

## [0.11.2] - 2026-07-19

### 乒乓视频循环

- 四段背景视频离线合成为“正放 → 倒放”，循环结束时回到原始第一帧，取消上一版可被察觉的双视频长交叉淡化。
- 重新编码为固定 24fps 的静音 H.264 MP4，并优化首帧关键帧和网络加载顺序；单个文件由原来的约 6–9MB 降至约 3–4MB。
- 视频循环周期扩大为原素材的两倍，倒放发生在素材内部，不依赖浏览器对负播放速度的支持。

### 雨夜音量

- 雨夜场景环境声增加独立 `0.42` 系数，实际输出约为原来的四成；其他三个场景保持现有音量。
- 独立 WAV 无缝循环和场景声音交叉淡化保持不变。

### 工程

- 移除双视频、逐帧边界监测和交叉淡化状态，降低同时解码两路高分辨率视频造成的资源占用。
- 更新自动化验证，检查视频静音、文件体积、乒乓播放结构和雨夜独立音量。

## [0.11.1] - 2026-07-19

### 无缝动态场景

- 单个硬循环视频改为两层视频轮换播放，在循环点提前 `1.25` 秒预播下一层并交叉淡化，隐藏短视频重新开始时的画面跳变。
- 使用逐帧视频回调监测循环边界，旧浏览器回退到动画帧检查；下一层未准备完成时保留原视频重播兜底。
- 两层视频始终静音，场景声音不再依赖 MP4 播放进度。

### 独立环境声

- 从四段场景视频提取原始音轨，采用切半重排和等功率交叉淡化制作四条无缝 WAV 环境音。
- 环境音由 Web Audio 独立解码循环，继续使用 `0.28` 默认音量；切换场景、开启和关闭时均进行 `0.8` 秒淡入淡出。
- 环境声与钢琴声音共用浏览器音频上下文，但不进入钢琴采样、发声和延迟测量链路。

### 工程

- 新增双视频、帧级循环监测、独立环境音资源及 WAV 文件有效性的自动化验证。

## [0.11.0] - 2026-07-19

### 曲库

- 接入项目所有者提供并明确允许发布的《蒲公英的约定》MIDI，内置可直接播放曲目增加至三首。
- 自动演奏速度扩展为 `0.75× / 1× / 1.25× / 1.5× / 2×` 五档，继续保持变速不变调。

### 场景环境声

- 启用四段背景视频自带的 AAC 音轨，以 `0.28` 音量作为轻柔白噪音氛围。
- 页面仍以静音视频开始；用户主动启动音频后才尝试开启环境声，避免突然出声并兼容浏览器自动播放限制。
- 顶部增加双语环境声开关，可独立静音，不影响自由演奏或 MIDI 自动演奏。

### 工程

- 环境声不接入低延迟 Web Audio 演奏链路，避免影响琴键调度。
- 新增第三首内置 MIDI、五档速度和环境声交互的自动化验证。

## [0.10.0] - 2026-07-19

### 通用本地 MIDI

- 欣赏模式增加独立的“导入本地 MIDI”入口，不再要求用户先选择一个预设曲名。
- 导入后自动使用文件名作为曲名，立即启用播放、暂停、进度跳转、循环、变速、琴键同步与跟弹。
- 文件通过浏览器 File API 在当前页面内读取和解析，不发送上传请求；刷新页面后需重新选择。

### 曲库调整

- 内置曲库收敛为两首真正可直接播放的曲目：《致爱丽丝》和《圣诞快乐，劳伦斯先生》。
- 移除仅有名称但无法直接播放的四个占位条目，避免用户误以为曲目已经可用。
- 接入项目所有者提供并明确允许发布的《圣诞快乐，劳伦斯先生》MIDI 文件。

### 工程

- 本地导入曲目使用独立的临时播放槽，不覆盖内置曲目的解析缓存。
- 更新双语界面、页面描述、使用说明与自动化验证。

## [0.9.0] - 2026-07-19

### 欣赏模式

- 新增双语钢琴曲库：圣诞快乐，劳伦斯先生、蒲公英的约定、致爱丽丝、你离开的事实与 Call of Silence。
- 新增 MIDI 自动演奏，支持播放/暂停、上一首/下一首、进度跳转、循环和 `0.75× / 1× / 1.25×` 速度。
- 自动演奏复用现有 Web Audio 采样并采用 25ms 调度循环与 160ms 前瞻窗口；用户键盘仍可同时跟弹。
- 自动音符使用白金色琴键与光尘，用户演奏继续使用当前场景强调色。

### 曲目与授权

- 《致爱丽丝》内置 Mutopia Project 的 Public Domain MIDI，并在曲库保留来源与授权链接。
- 其余四首受版权保护的曲目提供本地授权 MIDI 导入入口；文件仅在浏览器内解析，不上传服务器或加入发布包。

### 工程

- 引入 `@tonejs/midi` 解析标准 MIDI；解析仅在加载曲目或用户导入文件时发生，不进入自由演奏按键热路径。
- 欣赏模式与中文/英文界面完整联动，并补充可访问名称与自动化检查。

## [0.8.0] - 2026-07-19

### 品牌

- 产品名称升级为“乐境 MeloVista”，突出旋律与窗外风景共同构成的沉浸演奏空间。
- 中文界面使用“乐境 MeloVista”，英文界面使用“MeloVista”，替代实验性质较强的 Six Octave Piano Lab。

### 多语言

- 新增中文与英文完整界面，覆盖场景、音色、演奏模式、音频状态、性能信息、琴键辅助说明与无障碍文本。
- 顶部控制条增加语言切换，用户选择保存在当前设备，并同步更新页面语言和标题。
- 支持 `?lang=zh` 与 `?lang=en` 指定语言；没有明确选择时根据设备语言自动判断。

### 保持兼容

- 语言切换不进入 Web Audio 演奏热路径，不改变键盘映射、采样加载、延迟测量、视频循环与沉浸模式白名单。

## [0.7.0] - 2026-07-19

### 新增

- 四个场景接入本地静音循环视频：沧海听风、山湖静语、雨夜伴灯与暮光之城。
- 每个视频拥有独立的纵向构图焦点，让窗台物件自然停在钢琴上沿附近。
- 从视频画面生成四张 16:9 静态 poster，用于首屏、视频加载和播放失败兜底。

### 性能与可访问性

- 页面先显示静态 poster，客户端挂载后仅加载当前场景视频；场景切换不会同时加载四个视频。
- 视频固定静音，不进入 Web Audio 演奏链路；继续保持声音调度优先于光尘生成。
- 系统启用“减少动态效果”时不加载视频，仅使用静态场景。

### 视觉优化

- 暮光之城的音符光尘从原星空紫色调整为与落日一致的暖金橙色。

## [0.6.0] - 2026-07-19

### 新增

- 可视钢琴改为完整连续展示 C1–B6，共六组、72 个可见琴键。
- C1/C6 与 C2/C3 的切换逻辑保持不变，未启用音区仍显示但标记为“待切换”，避免重复发光。
- 音符光尘的横向起点同步扩展为六音区坐标。

### 视觉实验

- 保持 186px 琴体高度和现有标签字号，仅将横向布局从四等分改为六等分，用于验证完整六八度视图是否过密。
- 最小舞台宽度从 1060px 调整为 1260px，窄屏可横向滚动查看完整琴键。

## [0.5.1] - 2026-07-19

### 优化

- 将可切换低音组的默认音区从 C3–B3 调整为 C2–B2，使页面首次打开时左侧连续露出 C1–B2。
- 保留 `Space` 在 C2/C3 之间切换的原有能力。

## [0.5.0] - 2026-07-19

### 新增

- 增加可切换扩展音区，默认 C1–B1；全音映射为 `Insert Home PageUp NumLock / * -`，半音映射为 `Delete End Numpad 7 8 9`。
- 小键盘 `+` 可将扩展音区切换到 C6–B6，并将该组琴键移动到可视钢琴最右侧。
- 增加 C1 与 C6 音区的 Salamander Grand Piano 真实采样锚点，避免依赖跨整八度变调。
- 沉浸模式白名单加入全部 12 个扩展琴键和小键盘 `+`。

### 优化

- 可视钢琴改为四组并排，整体高度从 246px 缩至 186px，释放更多窗台与场景视野。
- 性能信息与光尘起点随琴体同步下移，继续保持音频优先于视觉粒子。

### 保持兼容

- 保留 `Space` C2/C3 切换、左 `Alt` 长短音、五种音色与四个沉浸场景。

## [0.4.0] - 2026-07-19

### 新增

- 在海岸午后之外增加林间晴窗、雨夜公寓与星空露台三个原创沉浸场景。
- 增加玻璃质感场景选择器，切换时同步改变背景、环境光与音符粒子颜色。
- 增加欣赏模式实施方案，覆盖 MIDI 曲库、自动演奏、用户跟弹、授权与验收标准。

### 优化

- 所有音高的光尘统一上升到相同最高处，并增强光点尺寸、拖尾、亮度和持续可见时间。
- 音色选择由浏览器原生下拉框改为与顶部控制条一致的自定义玻璃弹层。
- 新场景在页面空闲后预加载，场景资源使用本地压缩 WebP，不进入音频调度热路径。

### 保持兼容

- 继续保证声音先于视觉粒子启动，并保留三八度键位、五种音色、长短音、低音区切换与沉浸模式白名单。

## [0.3.0] - 2026-07-19

### 新增

- 增加首个场景皮肤“海岸午后”，使用原创阳光海景琴房背景、暖色玻璃控制条和临窗钢琴布局。
- 将水下气泡替换为随音高上升的金色音符光尘，粒子始终在音频启动之后创建。
- 将性能详情收纳到可展开面板，同时保留调度、P95 与当前音的紧凑演奏信息。
- 为场景增加社交分享元数据与本地压缩 WebP 预览图。

### 保持兼容

- 保留三八度键位、五种真实采样音色、`Space` 低音区切换、左 `Alt` 长短音切换及沉浸模式白名单。
- 保留短音约 2–3 秒、长音最长约 9 秒的自然尾音，不改变 Web Audio 低延迟热路径。

## [0.2.1] - 2026-07-19

### 修复

- 沉浸模式保留 `Space` 音区切换与左 `Alt` 长短音切换，它们与 36 个琴键一起归入钢琴相关操作。
- 仅屏蔽与钢琴演奏无关的其他按键。

## [0.2.0] - 2026-07-19

### 新增

- 增加沉浸模式入口，开启后仅 36 个钢琴映射键可以触发声音。
- 沉浸模式屏蔽 `Space`、左 `Alt` 及其他非琴键的页面默认行为，减少演奏误触。
- 支持的浏览器会尝试进入全屏并启用 Keyboard Lock；不支持时自动使用页面捕获拦截。
- 页面提供明确的退出按钮和模式状态提示。

### 工程

- 建立版本更新日志与 Git Tag 规范。

## [0.1.4] - 2026-07-19

- 将短音尾音调整为约 2–3 秒的自然衰减。

## [0.1.3] - 2026-07-18

- 将长音改为缓慢自然衰减，尾音最长约 9 秒。

## [0.1.2] - 2026-07-18

- 改为连续三八度真实钢琴键盘界面。
- 增加水中发光气泡动效。
- 增加原声、明亮、小提琴、钢丝弦吉他和萨克斯五种音色。

## [0.1.1] - 2026-07-18

- 接入真实钢琴采样。
- 增加左 `Alt` 长短音切换。

## [0.1.0] - 2026-07-18

- 完成三八度电脑键盘钢琴与低延迟验证页面。
